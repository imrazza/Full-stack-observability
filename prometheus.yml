global:
  scrape_interval: 5s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: "app"
    static_configs:
      - targets: ["app:8000"]

  - job_name: "node"
    static_configs:
      - targets: ["node-exporter:9100"]
